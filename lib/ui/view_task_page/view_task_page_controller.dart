import 'package:flutter/material.dart';

class ViewTaskPageController extends StatelessWidget {
  const ViewTaskPageController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
